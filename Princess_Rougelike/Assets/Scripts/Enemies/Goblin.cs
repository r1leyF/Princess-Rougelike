using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Goblin : MonoBehaviour
{
    /*
    //game object
    Vector3 playerPos;

    // Start is called before the first frame update
    void Start()
    {
        playerPos = GameObject.Find("Your_Name_Here").transform.position;
    }

    private void Update()
    {
        playerPos = GameObject.Find("Your_Name_Here").transform.position;
        if (playerPos <= 10)
        {

        }
    }
    */
}
